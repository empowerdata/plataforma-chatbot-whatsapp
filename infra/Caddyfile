# HTTPS automático (Let's Encrypt). O domínio vem do .env do compose.
# Só o painel fica exposto à internet — o Evolution é interno, só o painel
# fala com ele (mesmo princípio da instalação via Render).
{$APP_DOMAIN} {
	encode gzip
	reverse_proxy app:3000
}
